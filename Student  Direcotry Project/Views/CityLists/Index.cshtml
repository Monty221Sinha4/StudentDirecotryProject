@model IEnumerable<Student__Direcotry_Project.CityList>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutPage1.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.City)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.City)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Cty_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Cty_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Cty_ID })
        </td>
    </tr>
}

</table>
